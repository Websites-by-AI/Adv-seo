// This project uses the @tailwindcss/vite plugin (see vite.config.ts).
// PostCSS is intentionally left with no plugins so the CSS pipeline
// does not try to load @tailwindcss/postcss (which is a leftover
// artifact of the original Next.js template).
export default {
  plugins: {},
};
